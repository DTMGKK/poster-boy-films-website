{
  "name": "poster-boy-films-website",
  "version": "1.0.0",
  "description": "Poster Boy Films website — Eleventy + Decap CMS.",
  "private": true,
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve --port=8080",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "2.0.1"
  },
  "engines": {
    "node": ">=18"
  }
}
